数组，切片，map if语句，switch语句，for循环，break和continue，函数。值传递和引用传递，init函数和defer函数，结构体，结构体的继承和指针，tag，自定义类型和类型别名。接口。

协程，channel，select与协程超时处理，线程安全和sync.Map，异常处理，泛型，文件操作，单元测试。

反射获取值，修改值，反射操作结构体，反射实现转sql，网络编程。tcp，http，go程序部署。